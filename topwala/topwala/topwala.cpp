// topwala.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include<iostream>
using namespace std;
#include"top.h"
#include"isover.h"

int main()
{
	top *hat=new isover;
	hat->mymemory();
	hat->disk();
	hat->thisexam();
	top dog=*(new isover);
	dog.mymemory();
	dog.disk();
	dog.thisexam();
	system("pause>nul");
}

